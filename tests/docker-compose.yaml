version: "3.9"

services:

  # See: https://github.com/cty3000/docker-xcaddy
  xcaddy:
    image: ${CONTAINER_IMAGE?err}:latest
    restart: unless-stopped
    ports:
      - target: 8080
        published: 8080
        protocol: tcp
        mode: host
    entrypoint: /app/caddy
    command: run
